# Deploy Commands to Main Server

When ready to go live on the main server, run in the JRMA Shell:

```
node deploy-commands.js --guild --guildId 537887361292304385
```

Or swap `DEV_GUILD_ID` in `.env` to the main server ID.
